// import dynamic from 'next/dynamic'

const components = {
  // ZeroRuntimeButton: dynamic(() => import('./ZeroRuntimeButton')),
  // SprinklesButton: dynamic(() => import('./SprinklesButton')),
  // VariantsButton: dynamic(() => import('./VariantsButton/VariantsButton')),
  // Buttons: dynamic(() => import('./VariantsButton'))
}

export default components
